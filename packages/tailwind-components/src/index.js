const alert = required('./alert');
const appBar = required('./app-bar');
const avatar = required('./avatar');
const badge = required('./badge');
const breadcrumbs = required('./breadcrumbs');
const button = required('./button');
const card = required('./card');
const dropdown = required('./dropdown');

module.exports = {
  alert,
  appBar,
  avatar,
  badge,
  breadcrumbs,
  button,
  card,
  dropdown,
};
